{
  "private": true,
  "name": "websocket-disconnection-lambda",
  "version": "1.0.0",
  "description": "500 card game - lambdas",
  "repository": {
    "url": "https://github.com/kirbyjs/500"
  },
  "main": "index.js",
  "author": "Kirby",
  "license": "MIT",
  "scripts": {
    "build": "yarn run clean && cross-env NODE_ENV=production babel ./src -d lib  -x '.ts','.js'",
    "clean": "rm -rf ./lib",
    "eslint": "eslint . --ext .js,.ts --ignore-pattern lib",
    "test": "cross-env NODE_ENV=test jest",
    "tsc": "tsc",
    "verify": "run-p eslint tsc"
  },
  "dependencies": {
    "aws-sdk": "^2.624.0"
  }
}
