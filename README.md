# 500

### Contribution

#### Starting Locally

##### With docker-compose
`$ docker-compose up`

#### With Node.js
`$ npm run build:local` :notebook: this is only necessary the first time or if dependencies get updated
`$ npm run start:local`
